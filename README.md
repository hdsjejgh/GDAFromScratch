Gaussian Discriminant Analysis implemented from scratch in Python

Only works for gaussianly distributed data (obviously)

Can't predict on very extreme coordinates since, due to rounding errors, the gaussian function returns 0, which messes up the calculations

